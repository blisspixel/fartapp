# SCOPE

In scope: a CLI accepting an integer intensity 1–5 that prints a fart sound plus a parenthetical rating. Out of scope (not present in context): anything beyond the five-level intensity mapping — no audio playback, no GUI, no config, no inputs outside 1–5.

## Evidence
- README.md:3-8
- go.mod:1-3
