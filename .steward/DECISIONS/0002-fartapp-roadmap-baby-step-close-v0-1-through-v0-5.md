# 0002. Fartapp roadmap baby-step: close v0.1 through v0.5 in this sandbox.

v0.1 and v0.2 are ALREADY on master (validated: go test passes, go run . 3 prints braaap (respectable)). Do not re-implement parsing or Pick.

1. Mark v0.1 and v0.2 as [x] in .steward/ROADMAP.md.
2. Verify v0.3 rating layer and v0.4 flawlessness tests; implement only if a test proves a gap.
3. Verify v0.5 matches README; fix only if broken.
4. Delete tracked cov.out and coverage artifacts; add to .gitignore.
5. Update .steward/progress.json with honest progress toward 1.0.

Minimal diff. No AI attribution.

- **Status:** executed (tests pass)
- **Decided:** 2026-06-21T23:23:20Z
- **Verification:** proof gate: pass; differential: sustained (baseline pass → after pass); tests 1 → 1; confidence 0.40 - basis: +0.50 re-executed verification passed (harness ran it, not the worker's word); +0.10 baseline also passed: no regression introduced (not proof of new behavior); -0.20 independent checker's last verdict was a rejection; +0.00 acceptance criteria: 4 met, 0 failed, 1 abstained or pending; human confirmation needed
- **Evidence:** fd56b51cd3a5bb1ffa4bc4f86c821f0d24b252caceca4fb6325285ef46c2e601

## Why

Approved and executed through the proof-gated loop. Goal: Fartapp roadmap baby-step: close v0.1 through v0.5 in this sandbox.

v0.1 and v0.2 are ALREADY on master (validated: go test passes, go run . 3 prints braaap (respectable)). Do not re-implement parsing or Pick.

1. Mark v0.1 and v0.2 as [x] in .steward/ROADMAP.md.
2. Verify v0.3 rating layer and v0.4 flawlessness tests; implement only if a test proves a gap.
3. Verify v0.5 matches README; fix only if broken.
4. Delete tracked cov.out and coverage artifacts; add to .gitignore.
5. Update .steward/progress.json with honest progress toward 1.0.

Minimal diff. No AI attribution.
